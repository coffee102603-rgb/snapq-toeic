import streamlit as st
st.set_page_config(page_title="DEBUG MIN", layout="centered")
st.title("✅ DEBUG MIN APP")
st.write("If you can see this, Streamlit rendering is OK.")
st.success("Next: we will hunt CSS that hides everything.")
